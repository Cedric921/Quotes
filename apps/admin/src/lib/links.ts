/**
 * Where the landing page sends people.
 *
 * The store listings do not exist yet, so both point at the page's own
 * download section until they do — replace the two constants when the app
 * is live, nothing else references the stores.
 */
/** The site's own address: what absolute links, the OG card and the JSON-LD resolve against. */
export const SITE_URL = "https://focus-admin.vercel.app";

export const APP_STORE_URL = "#download";
export const PLAY_STORE_URL = "#download";
export const CONTACT_EMAIL = "contact@oderaformations.com";
